{
  "extends": "standard",
  "rules": {
    "comma-dangle": ["error", "always-multiline"],
    "prefer-const": "error",
    "semi": ["error", "always"],
    "sort-keys": "error",
    "sort-vars": "error"
  }
}
